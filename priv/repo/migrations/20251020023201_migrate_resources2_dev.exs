defmodule Sorteios.Repo.Migrations.MigrateResources2 do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    rename table(:rifas), :owner_id, to: :user_id

    drop constraint(:rifas, "rifas_owner_id_fkey")

    alter table(:rifas) do
      modify :user_id,
             references(:users,
               column: :id,
               name: "rifas_user_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end
  end

  def down do
    drop constraint(:rifas, "rifas_user_id_fkey")

    alter table(:rifas) do
      modify :user_id,
             references(:users,
               column: :id,
               name: "rifas_owner_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end

    rename table(:rifas), :user_id, to: :owner_id
  end
end
